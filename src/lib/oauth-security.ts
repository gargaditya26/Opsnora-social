import { createCipheriv, createDecipheriv, createHmac, randomBytes, timingSafeEqual } from "crypto";

function secret(){const value=process.env.OAUTH_TOKEN_ENCRYPTION_KEY;if(!value)throw new Error("OAUTH_TOKEN_ENCRYPTION_KEY is not configured");const key=Buffer.from(value,"base64");if(key.length!==32)throw new Error("OAUTH_TOKEN_ENCRYPTION_KEY must be 32 bytes in base64");return key}
export function encryptToken(value:string){const iv=randomBytes(12);const cipher=createCipheriv("aes-256-gcm",secret(),iv);const encrypted=Buffer.concat([cipher.update(value,"utf8"),cipher.final()]);return [iv.toString("base64url"),cipher.getAuthTag().toString("base64url"),encrypted.toString("base64url")].join(".")}
export function decryptToken(value:string){const [iv,tag,data]=value.split(".").map(x=>Buffer.from(x,"base64url"));const decipher=createDecipheriv("aes-256-gcm",secret(),iv);decipher.setAuthTag(tag);return Buffer.concat([decipher.update(data),decipher.final()]).toString("utf8")}
export function createOAuthState(payload:{uid:string;workspaceId:string;provider:string}){const body=Buffer.from(JSON.stringify({...payload,nonce:randomBytes(16).toString("hex"),expires:Date.now()+10*60_000})).toString("base64url");const signature=createHmac("sha256",secret()).update(body).digest("base64url");return `${body}.${signature}`}
export function verifyOAuthState(value:string){const [body,signature]=value.split(".");if(!body||!signature)throw new Error("Invalid OAuth state");const expected=createHmac("sha256",secret()).update(body).digest();const supplied=Buffer.from(signature,"base64url");if(expected.length!==supplied.length||!timingSafeEqual(expected,supplied))throw new Error("Invalid OAuth state");const payload=JSON.parse(Buffer.from(body,"base64url").toString()) as {uid:string;workspaceId:string;provider:string;expires:number};if(payload.expires<Date.now())throw new Error("OAuth state expired");return payload}
