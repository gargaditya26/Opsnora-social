"use client";
import Link from "next/link";import {useEffect,useState} from "react";import {Menu,X} from "lucide-react";import {brand} from "@/lib/config";import {marketingNav} from "@/lib/marketing";import {useAuth} from "./auth-provider";
export function MarketingNav(){const{isAuthenticated}=useAuth();const[open,setOpen]=useState(false);const[scrolled,setScrolled]=useState(false);useEffect(()=>{const fn=()=>setScrolled(scrollY>20);addEventListener("scroll",fn,{passive:true});return()=>removeEventListener("scroll",fn)},[]);return <header className={`marketing-nav ${scrolled?"scrolled":""}`}><div className="marketing-container nav-inner"><Link href="/" className="marketing-brand"><span className="brand-mark">O</span><span><strong>{brand.product}</strong><small>An OPSNORA product</small></span></Link><nav aria-label="Primary navigation">{marketingNav.map(x=><Link key={x.href} href={x.href}>{x.label}</Link>)}</nav><div className="nav-actions">{isAuthenticated?<Link className="button button-secondary" href="/dashboard">Dashboard</Link>:<><Link className="nav-login" href="/login?next=/dashboard">Login</Link><Link className="button button-primary" href="/login?mode=signup&next=/dashboard">Get started</Link></>}<button className="mobile-menu-button" aria-label="Open navigation" aria-expanded={open} onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div></div>{open&&<nav className="mobile-marketing-nav" aria-label="Mobile navigation">{marketingNav.map(x=><Link key={x.href} href={x.href} onClick={()=>setOpen(false)}>{x.label}</Link>)}<Link href={isAuthenticated?"/dashboard":"/login?next=/dashboard"} onClick={()=>setOpen(false)}>{isAuthenticated?"Dashboard":"Login"}</Link><Link href="/contact" onClick={()=>setOpen(false)}>Contact</Link></nav>}</header>}
