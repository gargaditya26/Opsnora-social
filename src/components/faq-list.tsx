"use client";import{useMemo,useState}from"react";import{ChevronDown,Search}from"lucide-react";import{faqs}from"@/lib/marketing";export function FaqList(){const[q,setQ]=useState("");const items=useMemo(()=>faqs.filter(x=>`${x.category} ${x.q} ${x.a}`.toLowerCase().includes(q.toLowerCase())),[q]);return <><label className="faq-search"><Search/><span className="sr-only">Search FAQs</span><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search questions"/></label><div className="faq-list">{items.map(x=><details key={x.q}><summary><span><small>{x.category}</small>{x.q}</span><ChevronDown/></summary><p>{x.a}</p></details>)}</div></>}
