import Link from "next/link";import type{ReactNode}from"react";import {ArrowRight,Check}from"lucide-react";import {features,plans}from"@/lib/marketing";
export function PageHero({eyebrow,title,description,children}:{eyebrow:string;title:string;description:string;children?:ReactNode}){return <section className="page-hero"><div className="marketing-container narrow"><span className="marketing-eyebrow">{eyebrow}</span><h1>{title}</h1><p>{description}</p>{children&&<div className="hero-actions">{children}</div>}</div></section>}
export function FeatureGrid({limit}:{limit?:number}){return <div className="feature-grid">{features.slice(0,limit).map(({slug,title,description,icon:Icon})=><article key={slug}><span><Icon/></span><h3>{title}</h3><p>{description}</p><Link href={`/features/${slug}`}>Explore feature <ArrowRight/></Link></article>)}</div>}
export function PricingGrid(){return <div className="pricing-grid">{plans.map(p=><article key={p.name} className={p.recommended?"recommended":""}>{p.recommended&&<span className="plan-label">Recommended</span>}<h2>{p.name}</h2><p>{p.description}</p><strong>Flexible rollout</strong><small>Contact us for current plan availability.</small><ul>{p.features.map(x=><li key={x}><Check/>{x}</li>)}</ul><Link className={`button ${p.recommended?"button-primary":"button-secondary"}`} href={p.href}>{p.cta}</Link></article>)}</div>}
export function Cta(){return <section className="marketing-cta"><div><span className="marketing-eyebrow">Start with clarity</span><h2>Bring your social workflow into one focused workspace.</h2><p>Create an account or explore the product with our team.</p></div><div><Link className="button button-primary" href="/login?mode=signup&next=/dashboard">Get started</Link><Link className="button button-secondary" href="/contact">Contact us</Link></div></section>}
