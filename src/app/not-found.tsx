"use client";import Link from"next/link";import{useAuth}from"@/components/auth-provider";export default function NotFound(){const{isAuthenticated}=useAuth();return <main className="error-page"><span className="brand-mark">O</span><small>404</small><h1>That page is not in this workspace.</h1><p>The address may have changed or the page may no longer exist.</p><div><Link className="button button-secondary" href="/">Go home</Link>{isAuthenticated&&<Link className="button button-primary" href="/dashboard">Go to dashboard</Link>}</div></main>}
