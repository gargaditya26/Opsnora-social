import type{MetadataRoute}from"next";import{features,siteUrl}from"@/lib/marketing";export default function sitemap():MetadataRoute.Sitemap{const routes=["","/features","/platforms","/pricing","/solutions","/about","/contact","/faq","/privacy","/terms","/security",...features.map(x=>`/features/${x.slug}`)];return routes.map(route=>({url:`${siteUrl}${route}`,lastModified:new Date(),changeFrequency:route===""?"weekly":"monthly",priority:route===""?1:.7}))}
