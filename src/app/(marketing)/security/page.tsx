import{PageHero}from"@/components/marketing-sections";const items=[["Authentication","Firebase Email/Password authentication, persistent sessions and protected application routes."],["OAuth security","Signed, expiring state values and server-side code exchange protect social connection flows."],["Token handling","Provider tokens are encrypted before Firestore storage and are never returned to browser clients."],["Data access","Firestore rules deny by default and scope supported records to authenticated users or workspace membership."],["Secrets","Admin credentials, encryption keys and provider secrets remain server-only environment variables."],["Infrastructure","The application runs on Vercel and uses Firebase services currently configured for the project."]];export default function Security(){return <><PageHero eyebrow="Security" title="Practical safeguards, described without inflated claims." description="Security is built into authentication, API boundaries, token handling and data rules."/><section className="marketing-section"><div className="marketing-container security-grid">{items.map(([n,d])=><article key={n}><h2>{n}</h2><p>{d}</p></article>)}</div><p className="marketing-container legal-note">This page does not claim independent certifications or audits that have not been completed.</p></section></>}
