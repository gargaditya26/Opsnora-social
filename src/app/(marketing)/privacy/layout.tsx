import type { Metadata } from "next";
export const metadata: Metadata = { title: "Privacy policy", description: "How OPSNORA Social handles account, workspace and integration data.", alternates: { canonical: "/privacy" } };
export default function Layout({children}:{children:React.ReactNode}){return children}
