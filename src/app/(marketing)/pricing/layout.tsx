import type { Metadata } from "next";
export const metadata: Metadata = { title: "Pricing", description: "Explore configurable OPSNORA Social rollout plans for individuals and teams.", alternates: { canonical: "/pricing" } };
export default function Layout({children}:{children:React.ReactNode}){return children}
