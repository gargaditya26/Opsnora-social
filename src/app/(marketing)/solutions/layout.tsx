import type { Metadata } from "next";
export const metadata: Metadata = { title: "Solutions", description: "Social media workflow solutions for businesses, agencies, creators and marketing teams.", alternates: { canonical: "/solutions" } };
export default function Layout({children}:{children:React.ReactNode}){return children}
