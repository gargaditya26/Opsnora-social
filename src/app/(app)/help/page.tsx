"use client"; import { useState } from "react"; import { Modal } from "@/components/ui";
const guides=[{title:"Getting started",text:"Create your workspace, review Connections, then use Create Post to upload media, write a caption and choose a publishing time."},{title:"Connect social accounts",text:"Open Connections and choose Connect or Reconnect. Demo mode enables local workflow testing; production OAuth requires credentials from Meta and LinkedIn."},{title:"Create and schedule posts",text:"Add an image, select at least one connected platform, write your caption and choose Publish now or Schedule for later. Saved content appears across all product screens."},{title:"Troubleshoot publishing",text:"Open Failed posts from the dashboard, correct the connection or content issue and retry. Sensitive provider errors remain server-side."}];
export default function Help(){const [guide,setGuide]=useState<(typeof guides)[number]|null>(null);return <div className="page"><div className="page-header"><div><span className="eyebrow">Support</span><h1>How can we help?</h1><p>Guidance for creating, scheduling and publishing content.</p></div></div><div className="help-grid">{guides.map(x=><article key={x.title}><h2>{x.title}</h2><p>Learn how to use OPSNORA Social confidently.</p><button className="text-button" onClick={()=>setGuide(x)}>View guide →</button></article>)}</div>{guide&&<Modal title={guide.title} onClose={()=>setGuide(null)}><div className="guide-content"><p>{guide.text}</p><button className="button button-primary" onClick={()=>setGuide(null)}>Got it</button></div></Modal>}</div>}
