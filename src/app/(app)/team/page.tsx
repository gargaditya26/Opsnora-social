"use client";
import { useState } from "react"; import { UserPlus } from "lucide-react"; import { Modal, Button } from "@/components/ui"; import { useAppStore } from "@/components/app-store";
export default function Team(){const store=useAppStore();const [open,setOpen]=useState(false);const [name,setName]=useState("");const [email,setEmail]=useState("");const [role,setRole]=useState("Editor");function invite(){if(!name.trim()||!email.includes("@"))return;store.addMember({name,email,role});store.addNotification("Team invitation sent",`${email} was invited as ${role}.`);setOpen(false);setName("");setEmail("")}return <div className="page"><div className="page-header"><div><span className="eyebrow">Workspace</span><h1>Team</h1><p>Manage who can create, review and publish content.</p></div><button className="button button-primary" onClick={()=>setOpen(true)}><UserPlus size={16}/> Invite member</button></div><div className="table-card"><table><thead><tr><th>Member</th><th>Role</th><th>Status</th><th>Last active</th></tr></thead><tbody>{store.members.map(m=><tr key={m.id}><td><div className="member-cell"><span className="avatar">{m.name.split(" ").map(x=>x[0]).join("").slice(0,2)}</span><div><strong>{m.name}</strong><small>{m.email}</small></div></div></td><td>{m.role}</td><td><span className={`connection-state ${m.status==="Active"?"connected":"expired"}`}>{m.status}</span></td><td>{m.status==="Active"?"Now":"Invitation pending"}</td></tr>)}</tbody></table></div><div className="role-note"><h2>Workspace roles</h2><p><strong>Owner</strong> has full control. <strong>Admins</strong> manage content and settings. <strong>Editors</strong> create and schedule content. <strong>Viewers</strong> have read-only access.</p></div>{open&&<Modal title="Invite team member" onClose={()=>setOpen(false)}><div className="modal-form"><label className="field"><span>Full name</span><input value={name} onChange={e=>setName(e.target.value)} placeholder="Team member name"/></label><label className="field"><span>Email address</span><input value={email} onChange={e=>setEmail(e.target.value)} type="email" placeholder="name@company.com"/></label><label className="field"><span>Role</span><select value={role} onChange={e=>setRole(e.target.value)}><option>Admin</option><option>Editor</option><option>Viewer</option><option>Approver</option></select></label><div className="modal-actions"><Button variant="secondary" onClick={()=>setOpen(false)}>Cancel</Button><Button onClick={invite} disabled={!name.trim()||!email.includes("@")} >Send invitation</Button></div></div></Modal>}</div>}
