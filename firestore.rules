rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // OAuth tokens are read and written only by the trusted Firebase Admin SDK.
    match /{document=**} { allow read, write: if false; }
  }
}
